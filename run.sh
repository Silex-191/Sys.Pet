#!/bin/bash
# Активируем виртуалку
source venv/bin/activate

# Запускаем сервер
# --reload значит, что если ты изменишь код, сервер сам перезагрузится
echo "🚀 Запускаю SysPet на порту 8000..."
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
